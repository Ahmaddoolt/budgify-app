<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.budgifydev.budgify"> <!-- !!! YOUR PACKAGE NAME HERE !!! -->

    <!-- === Required Permissions === -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/> <!-- If targetSdk 34+ for AlarmService -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT"/>
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <application
        android:label="Budgify"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="portrait"
            android:showWhenLocked="true"
            android:turnScreenOn="true">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Intent for when MainActivity is launched by an alarm -->
            <intent-filter>
                <action android:name="com.example.budgify.ALARM_TRIGGER"/> <!-- !!! USE YOUR PACKAGE NAME !!! -->
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
            <!-- For AwesomeNotifications body tap -->
            <intent-filter>
                <action android:name="FLUTTER_NOTIFICATION_CLICK"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <!-- Alarm Package Components (com.gdelataillade.alarm) -->
        <service
            android:name="com.gdelataillade.alarm.services.AlarmService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback"
            android:stopWithTask="false"/>
        <receiver
            android:name="com.gdelataillade.alarm.receivers.AlarmReceiver"
            android:enabled="true"
            android:exported="false"> <!-- Keep false unless system needs to directly access it beyond BOOT_COMPLETED -->
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <!-- Awesome Notifications Components -->
        <receiver
            android:name="com.awesome.notifications.notifications.core.broadcasters.ScheduledNotificationReceiver"
            android:exported="false"/>
        <receiver
            android:name="com.awesome.notifications.notifications.core.broadcasters.DismissedNotificationReceiver"
            android:exported="false"/>
        <receiver
            android:name="com.awesome.notifications.notifications.core.broadcasters.ActionReceiver"
            android:exported="false"/>
        <receiver
            android:name="com.awesome.notifications.notifications.core.broadcasters.BootReceiver"
            android:enabled="true"
            android:exported="true"> <!-- Must be exported to receive system broadcasts -->
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <service
            android:name="com.awesome.notifications.notifications.core.background.NotificationBackgroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="shortService"
            android:stopWithTask="false"/>

        <!-- Flutter and AdMob config -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
        <!-- Replace with your actual AdMob App ID for release -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713"/>

    </application>

    <!-- For apps targeting Android 11 (API level 30) or higher,
         if you need to query or interact with specific packages -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Add other query intents if your app needs them, e.g., for specific browsers or mail clients -->
    </queries>

</manifest>